<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="35" />

    <!-- Network and notifications -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground services if needed -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

	<application
 	  android:name=".App"
  	  android:label="@string/app_name"
  	  android:icon="@mipmap/ic_launcher"
  	  android:theme="@style/Theme.GitHubUnofficial"
  	  android:supportsRtl="true"
  	  android:usesCleartextTraffic="false"
   	  android:fullBackupContent="true">

        <!-- Main entry point -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WebView content host -->
        <activity
            android:name=".webview.MarkdownActivity"
            android:exported="false"
            android:label="Markdown" />

        <!-- Additional Activities -->
        <activity android:name=".features.issues.IssuesActivity" android:exported="false"/>
        <activity android:name=".features.prs.PullRequestsActivity" android:exported="false"/>
        <activity android:name=".features.repos.RepositoryActivity" android:exported="false"/>

        <service android:name=".services.SyncService" android:exported="false"/>

    </application>
</manifest>